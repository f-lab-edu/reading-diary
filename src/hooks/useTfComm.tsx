import { useState } from 'react';

interface useTfCommProps {}

const useTfComm = ({ value }: any) => {
  const [tfValue, setTfValue] = useState<string>('');

  return '';
};

export default useTfComm;
